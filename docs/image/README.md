# Image Files

Place image files here with numeric names: `1.png`, `2.png`, `3.png`, etc.

These are referenced by `imageId` in `python_course/data.json` using `"type": "image"`.

Example data.json entry:
```json
{
  "type": "image",
  "imageId": 1,
  "caption": "Description of the image"
}
```
